#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

set -e

echo "-----> Installing OpenSCAD Binaries"
cd $1
mkdir vendor
cd vendor
wget http://files.openscad.org/openscad-2014.03.x86-64.tar.gz
tar -xvf openscad-2014.03.x86-64.tar.gz
